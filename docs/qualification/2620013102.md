# Qualification record 2620013102

Qualification completed July 17, 2026 for Taskbar Thumbnail Reorder 0.1.1 on Windows 11 Insider
25H2 build 26200.8875.

## Exact compatibility scope

The selected backend is non-animated XAML. This qualification applies only when both module
identities match exactly:

| Module | Path | Timestamp | SizeOfImage | PDB GUID | Age | Module SHA-256 |
| --- | --- | ---: | ---: | --- | ---: | --- |
| `taskbar.dll` | `C:\Windows\System32\Taskbar.dll` | 2766838074 | 3129344 | `{E6BFF37F-792C-E007-D441-6BBD8DDEAFDA}` | 1 | `27829D594AA4E7A00A2317802A9AA20E66A3E2811832AAC785FD86150A28B9A2` |
| `Taskbar.View.dll` | `C:\Windows\SystemApps\MicrosoftWindows.Client.Core_cw5n1h2txyewy\Taskbar.View.dll` | 1781122501 | 10051584 | `{4A99B7C5-BAD9-4999-9575-72CC66E11978}` | 1 | `00D1BD68240ED0CDB19A98E551BC5BFBA383843CC2564FF40523CB2DCFCD09F5` |

Both Microsoft PDBs matched their DLL CodeView GUID and age exactly before DIA enumeration. All 14
required XAML symbols resolved uniquely and passed section-permission validation. The updated
`taskbar.dll` RVAs were regenerated rather than copied. The XAML constructor still supplies the
`ITaskListUI` context used by the thumbnail mapping; no classic-backend base adjustment is present.

## Signed compatibility data

Sequence 2 retains record `2620013101` and adds exact record `2620013102`. The ECDSA P-256 private
key remained outside the repository.

| File | SHA-256 |
| --- | --- |
| `records/2620013102.json` | `324EB2FD6B0B75822507CCCEBC1FEFB5E164D55D69A27709AD2AEB9CDAAD0F2D` |
| `compat.bin` | `2E17ED211B37252497490147FF054E994E2D8B5ED243864B207AE6A4E77E5EF8` |
| `compat.sig` | `10F08A16C9E407939A0338A77A88F937E254D51669B2FF662F9DB22FD331E449` |
| `manifest-public-key.bin` | `0661465A3B6B57391A6AD8B51F0EDCBA661A760FFD55752300E21AFC13BDCC2B` |

Offline diagnostics verified the signature, exact module identities, record `2620013102`, the XAML
backend, and every RVA. The same pair was downloaded from the public feed and compared byte-for-byte.

## Updater and live evidence

- A v0.1.1 test build containing only embedded sequence 1 started Unsupported on this taskbar build.
- Its automatic one-time check downloaded sequence 2 from the configured public HTTPS feed, verified
  and atomically installed it, reloaded the manifest, selected record `2620013102`, and reached Active
  without restarting TTR.
- The payload loaded in the expected Explorer process with the XAML backend and Explorer remained
  responsive.
- Normal thumbnail clicking and hover previews remained functional.
- The user directly performed the drag test with distinguishable application windows and confirmed
  that thumbnail reordering works on this exact build.
- Explorer restart and automatic reattachment completed successfully with the external sequence-2
  manifest still selected.

This is focused exact-build evidence, not a claim about other Windows builds, taskbar implementations,
monitor arrangements, or DPI configurations. Any module-identity change remains fail closed until a
separately generated, signed, and qualified record is published.
