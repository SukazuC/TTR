# Testing

Automated tests exercise manifest bounds, corruption handling, and protocol layouts without touching Explorer. Manual qualification must cover click preservation, adjacent drag moves, close buttons, Aero Peek, multiple monitors, DPI variants, Explorer restart, disable/re-enable, and an intentionally unsupported module identity.

Never run unreviewed generated RVAs against the real taskbar. A symbol match alone is not behavioral compatibility.
